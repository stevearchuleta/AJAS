# Golden outputs

`stage1-greenhouse-ready-for-review.json` is the exact canonical Prompt-5 result. It includes deterministic state history, policy/liveness decisions, explainable fit, packet metadata, byte-readback hashes, and audit events. Updates must be intentional and accompanied by fixture-hash and test review.
