# Database package

Reserved for PostgreSQL schemas, migrations, repositories, and transactional outbox code after the database-provider and ORM ADR is approved. No database connector is present in Stage 0B.
